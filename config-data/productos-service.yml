server:
  port: 8082
spring:
  application:
    name: producto-service
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL8Dialect
  datasource:
    url: jdbc:mysql://localhost:3306/producto-service?serverTimezone=UTC
    username: root
    password: 
eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka